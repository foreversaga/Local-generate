import { LibraryWorkspace } from "../../components/library/LibraryWorkspace";
import { RoutePage } from "../../components/shell/RoutePage";
export default function LibraryPage(){return <RoutePage eyebrow="Library" title="素材與輸出" description="管理 input/output、搜尋、預覽、下載、刪除、批次與上傳；Create 只使用精簡 Asset Picker。"><LibraryWorkspace/></RoutePage>}
